{
  "name": "@guardrail/core",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --dts-resolve --clean",
    "lint": "eslint src test --max-warnings=0",
    "test": "node --test test/policy.test.mjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.1.5"
  }
}
